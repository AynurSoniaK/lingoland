{% extends "base.html.twig" %}

{% block title %}{{ parent() }} | Dashboard | 

{% if modification %}
    Modifier la communication "{{ communication.name }}"
{% else %}
    Ajouter une disponibilité
{% endif %}

{% endblock %}


{% block body %}
{% if modification %}
    <h1 class="h1_profil2 col-md-12 d-flex">Modifier la communication "{{ communication.name }}"</h1>
{% else %}
    <h1 class="h1_profil col-md-12 d-flex">Ajouter une communication</h1>
{% endif %}


    <div class="col-md-5 mx-auto">

        {{ form_start(formCommunication) }}

            {{ form_row(formCommunication.name) }}


        </div>
<div class="col-md-12 d-flex justify-content-center">
            <input type="submit" 

            {% if modification %}
              class="btn btn-success2"  value="modifier"
            {% else %}
              class="btn btn-success"  value="Ajouter"
            {% endif %} 
            > 
            
            



    </div>
    {{ form_end(formCommunication) }}

{% endblock %}

